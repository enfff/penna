[package]
name = "penna-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", default-features = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
penna-core = { path = "../core" }
penna-adapters-git = { path = "../adapters/git" }

[dev-dependencies]
git2 = "0.19"
tempfile = "3"
