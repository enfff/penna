---
description: Conventional commit message from staged diff
---
Generate a conventional commit message from the staged diff only (run `git diff --cached`). Stage nothing yourself. Format: type(scope): summary, body only if why is non-obvious. Output the message ready for `git commit -F -`. $ARGUMENTS
