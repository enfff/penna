# Legacy Alias: dev-workflow

This legacy command is preserved.
Use GitHub Copilot prompt:
- .github/prompts/dev-workflow.prompt.md

Behavior:
- Enforce ADR-first workflow.
- Use branch naming convention.
- Sequence implementation, arch check, tests, commit, merge.
