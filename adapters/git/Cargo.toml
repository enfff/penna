[package]
name = "penna-adapters-git"
version = "0.1.0"
edition = "2024"

[dependencies]
penna-core = { path = "../../core" }
git2 = "0.19"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
