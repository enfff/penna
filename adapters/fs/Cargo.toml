[package]
name = "penna-adapters-fs"
version.workspace = true
edition.workspace = true
description = "Filesystem adapter for Penna journaling engine"
license.workspace = true

[dependencies]
penna-core = { version = "0.1.0", path = "../../core" }
